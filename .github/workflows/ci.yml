name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

defaults:
  run:
    working-directory: packages/statemachine

jobs:
  tier-a-bun:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with: { bun-version: latest }
      - run: bun install
        working-directory: .
      - run: bun run check
      - run: bun run build
      - run: bun run test
      - run: bun run knip
      - run: bun run test:cjs-smoke
      - run: node test/verify-dist.cjs

  tier-a-node:
    # Latest LTS only (Node 24). Older lines (18/20/22) are intentionally NOT
    # supported — see package.json engines (>=24). All gates run on this single
    # leg, so the former node-18/20 matrix and its per-version `if:` guards are
    # gone.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24 }
      - run: npm install --no-workspaces
      - run: npm run check
      - run: npm run build
      - run: npm test
      - run: npm run knip
      - run: npm run test:cjs-smoke
      - run: node test/verify-dist.cjs
      - run: npm run test:coverage
      - run: npm run api:check
      - run: git diff --exit-code etc/statemachine.api.md
      # --- TASK-014 Step 11 (B/C): DST PR-fast gate ---
      # Bounded safety+liveness+coverage+perf-smoke budget (incl. an ISS-030
      # async-action witness). The heavy nightly sweep stays in sim-nightly.yml.
      - run: SM_SIM=1 SIM_SEEDS=64 SIM_STEPS=200 npm run sim:pr
      - run: npm run api:check:sim
      - run: git diff --exit-code etc/statemachine-sim.api.md
      # Core-bundle byte/hash guard (D8/R23): dist/index.{js,cjs} byte-stable.
      # Baseline is measured on Node 24 (== this leg); refresh with
      # SM_DIST_UPDATE_BASELINE=1 on a Node 24 runner.
      - run: node test/verify-dist-bytes.cjs
      # SM_SIM-gated COMPILE-LEVEL guards. These spawn `tsc` (~8s total), so they
      # are not on the unconditional `npm test` legs — but SM_SIM=1 previously
      # reached them on NO leg at all: the only SM_SIM step is `sim:pr`, and that
      # script scopes vitest to one unrelated file. The guards themselves still
      # bit through the project-wide `tsc`; what was dormant was the PROOF that
      # they bite. Runs after `npm run build` because the byte guard now FAILS on
      # an unbuilt tree instead of skipping. Asserted by an ungated test in
      # wire_seam_forwarding.test.ts so this step cannot be dropped silently.
      - run: npm run sim:guards

  tier-b-deno:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24 }
      - uses: denoland/setup-deno@v1
        with: { deno-version: vx.x.x }
      - run: npm install --no-workspaces
      - run: npm run check
      - run: npm run build
      - run: npm run test:deno-smoke

  tier-b-browser:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24 }
      - run: npm install --no-workspaces
      - run: npm run check
      - run: npm run build
      - run: npx playwright install chromium --with-deps
      - run: npm run test:browser
