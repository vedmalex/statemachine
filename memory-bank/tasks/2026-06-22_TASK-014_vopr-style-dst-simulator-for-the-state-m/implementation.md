---
event_store_revision: 16
event_store_hash: bdde9266a73ee96aa76976fb75a6f47349910ffaaef0ab0d35ace0ee15724e5a
materialized_at: 2026-06-22T14:21:53.518Z
materialized_by: phaseTransition
---
# Implementation — TASK-014

## Changes Made

Step 1 — PRNG + trace + clock determinism substrate. Created src/sim/prng.ts (FROZEN splitmix64 mixer + 64-bit FNV fork combine + rejection-free Lemire int + makePrng bigint|string canonical / validated number overload + SimError), src/sim/clock.ts (forward-only monotonic SimClock, Clock-assignable now()), src/sim/trace.ts (CLOSED TraceFrame two-orthogonal-unions, hashTrace key-sorted content-only, normalizeParts default-sort matching state_machine.ts:639, configHash structural toString-fold avoiding createdAt leak, PRNG_VERSION const), src/sim/index.ts (stub barrel re-exporting VALUE symbols). Golden vectors verified against PLAN-pinned values: seed=0 draws, fnv64('topology')=0x516188458c0dece4, fork non-advance, snapshot/restore draw[2], int(100) seq. knip.json: added src/sim/index.ts as production entry (TECH_SPEC §8(4) sanctioned barrel-reachability fix; not a suppression). NO core engine file modified; etc/statemachine.api.md zero diff.

Step 3 (Driver + settle primitive + post-construction drain): Added src/sim/env.ts (Env/SchedulerView + inFlightAsyncCount counter + bracketAsync wrapper + makeObservableScheduler shim over createVirtualScheduler with lazy-cancel parity), src/sim/settle.ts (the SOLE settleMacrostep converged-macrostep primitive: microtask-drain-to-quiet THEN process-due-timers ordering, 4-conjunct quiescence predicate incl. inFlightAsyncCount===0, policy-parameterized single clock-jump for liveness, WAITING_ON_TIMER/WAITING_ON_TRANSITION_TIMEOUT/microtask-budget findings, DEFAULT_MAX_TURNS=1024), src/sim/driver.ts (SimDriver: 5-seam wiring, capture-seam wrap, function-valued invoke-action bracketing, mandatory post-construction safety drain recording frame 0, fixed macrostep ordering with 3 fire-outcome classification + explicit-Adapter 2nd-positional, per-transition Adapter-seam frames + 1 settle-boundary backstop frame, errorState/transitionTimeout passthrough). Extended src/sim/index.ts barrel with Step-3 VALUE symbols for knip reachability. Tests: src/tests/sim/settle.test.ts (11) + src/tests/sim/driver.test.ts (23). All 109 sim tests green; tsc clean; npm run check (biome+tsc+knip) green. Zero core engine files modified.

- Step 4 (Scenario generator) implemented under src/sim/**: scenario.ts (frozen ScenarioSpec/Op/Bounds/TopologySpec + co-located FaultPlan stub), topology.ts (genConfig — correct-by-construction validateConfig-clean machine: non-numeric names, parallel regions w/ shared-leaf pinned initial, history shallow/deep, final leaves + done.state join, closure-free literal-inlined guards/actions/conds incl >=1 literal-false invoke.cond and >=1 closure-free async action), ops.ts (genOps — closed-loop available-event-aware op stream w/ stable op-N ids, pBlind reject path, no local drain), define.ts (toEngineConfig via security.ts:640-mirroring recreateLiteral, runScenario/generateScenario/defineScenario/pinTopology/pinOps).
- Extended src/sim/index.ts barrel with Step-4 VALUE symbols for knip reachability.
- Tests: src/tests/sim/generator.test.ts (256-seed correct-by-construction + warning whitelist, closure-free re-eval via exact executor signature, non-numeric names + stable region init, literal-false cond, stable op-ids survive removal, non-vacuous Bounds clamp [depth=error/count=warning], closed-loop genOps pBlind=0/>0, async-action emit+behavioral done, errorState excluded, JSON round-trip) and replay.test.ts (same-spec identical traceHash 16-seed sweep, deterministic generateScenario, JSON-round-trip identical traceHash, distinct-seed sanity).

Step 5 (Fault layer) — wrote src/sim/faults.ts (seven-kind FaultKind taxonomy + plain InjectedFault + FaultRecord + per-opportunity FaultCursor + structural errorClass classifier classifyError/captureRejection reading context fields/identity NEVER message + corrupt-state probe types I6_PROBE/I10_PROBE + classifyCorruptState), src/sim/harness.ts (callback throw injection via config-mutation wrapThrowingCallback observed at harness boundary, applyThrowFaults wrapping function-valued invoke.action/guard/onTransition with string-method uncovered/N-A markers for ISS-029, external-buffer reorder/drop/dup applyQueueFaults + overflow flood, fireBuffered with deterministic sync-at-enqueue tick detection for overflow-vs-depth identity classification + explicit-Adapter 2nd-positional, inFlightAsyncCount bracket reuse incl Promise.race action-wrapper finally, corrupt-state issuance issueCorruptStateWrite through wrapped Adapter.set), src/sim/observable-scheduler.ts (ObservableScheduler decorator over createVirtualScheduler with site-keyed jitter via fork('jitter:'+timerSiteId), eff=max(0,delay+j), executeAt mirror, lazy-cancel parity, pendingCount/earliestExecuteAt/schedulerEmptyAt). Extended src/sim/index.ts barrel with Step-5 VALUE symbols for knip reachability; narrowed src/sim/scenario.ts to import FaultPlan from faults.ts (single definition; no divergence).

TASK-014 Step 6 (Invariants + Liveness + Fairness oracles) — GREEN. Added 4 production files under src/sim and 2 test files under src/tests/sim; extended src/sim/index.ts barrel. tsc clean, npm run check (biome+tsc+knip) green, all 226 sim tests pass (12 files), 50 new Step-6 tests pass, full package 611 tests pass (zero regressions). No core engine file touched (zero ABI/dist diff). Files: src/sim/invariants.ts (FROZEN Invariant/Violation/CheckerContext/ConfigGraph/FinalState types + buildConfigGraph replicating private getRegionKey + I-1..I-12 registry), src/sim/invariants.runner.ts (BLIND runSafety — no I-\d+ literal, mechanical grep), src/sim/liveness.ts (5-kind Quiescence + fire-boolean demotion + K=states.length+1 cycle detector + TIMEOUT_BUDGET), src/sim/fairness.ts (eventually-healthy FaultSchedule + progress-blocking vs perturb-only classification). ISS-031 (corrupt-state I-6 via restore path :734 / I-10 via getCurrentState :1219, both via real engine), ISS-033 (non-vacuous ≥2-region + same-depth done.state pins) discharged. Coverage: invariants.ts 99.34/92.37/100/99.31, invariants.runner.ts 96/94.11/100/95.65, liveness.ts 100/97.36/100/100, fairness.ts 100/100/100/100 — all ≥90/90/90/90.

- TASK-014 Step 7 (Shrinker + repro codegen): added src/sim/shrinker.ts (ddmin M0-M5 + R15 full-fingerprint predicate + R7 cache canonicalizer folding callback source strings + R22 fault op-id re-keying + always-valid bounded budget) and src/sim/repro-codegen.ts (MinimalRepro JSON record + runnable *.repro.test.ts emitter, public-import-only, no flush). Extended src/sim/index.ts barrel with Step-7 VALUE re-exports for knip reachability. Added tests src/tests/sim/shrinker.test.ts (35 tests) and src/tests/sim/repro-codegen.test.ts (11 tests). npm run check green; full sim suite 272/272; coverage shrinker.ts 94.79/90.24/100/94.66, repro-codegen.ts 100/100/100/100. Core engine untouched; etc/ zero diff.

Step 8 (perf/load metrics plane): added src/sim/metrics.ts (PerfSample/PerfReport/PerfBaselineFile shapes; hrtime throughput PRIMARY; SimMonitor.getDurations() advisory ms-coarse latency; node-guarded heap/gcProxy; median-of-N=5; PERF_REGRESSION_CONFIG 20/25/30 bands + zero-tolerance traceLen; two-sided loadPerfBaseline non-zero-p99 throw; evaluatePerfBands; PerfHarness; toBaselineFile/maybeWriteBaseline with SM_PERF_UPDATE_BASELINE gate). Added src/sim/perf-run.ts (perfMain CLI orchestrator). Added etc/sim-perf.baseline.json (REAL node v24 medians, --expose-gc: eventsPerSec=13310.93, traceLen=560, memoryGated:true, latencyGated:false honest). Added src/tests/sim/metrics.test.ts (32 tests) + src/tests/sim/metrics.perf.test.ts (SM_SIM-gated heavy run). Extended src/sim/index.ts barrel. Added package.json sim:perf + sim:perf:baseline scripts (vitest-based, env-gated, --expose-gc).

Step 10 (Public ./sim API surface + build wiring + ABI/dist zero-diff guards): completed/closed the Step-10 deliverables. (1) Fixed SimOptions to the frozen TECH_SPEC §1 shape: invariants?: readonly Invariant[] (was readonly unknown[]) and added faults?: FaultPlan in src/sim/public.ts. (2) DECIDED ISS-043 as BRANCH B (documented coupling — the recorded default per TECH_SPEC §6); committed the decision note in src/sim/index.ts and a falsifiable acceptance test src/tests/sim/tsconfig_isolation.test.ts (emission feasible via shared tsconfig.build.json; a src/sim type error reaches the shared tsc --noEmit graph, proving isolation deliberately NOT installed). (3) Added §6 vitest-coverage decision: excluded src/sim/** from the core coverage gate in vitest.config.ts (sim:coverage CLI owns it). (4) Authored src/tests/sim/wire_settle.test.ts (DoD 9 grep+behavioral that Simulator.init/step/run delegate to the single settleMacrostep with no flush(N); DoD 15/ISS-030 public-path opaque-async settle determinism). (5) Cleaned knip.json (removed redundant src/sim/index.ts! entry flagged as redundant by knip; kept coverage.ts! entry).

Step 11 (CI wiring) — DST suite wired into CI without perturbing fast legs. (A) Env-gate: heavy runner suites behind describe.skipIf(!SM_SIM); determinism-floor tests (AC-1 canary, prng/sweep-shard/ci-gating) stay ungated. (B) ci.yml tier-a-node appends node-20-ONLY steps: `SM_SIM=1 SIM_SEEDS=64 SIM_STEPS=200 npm run sim:pr`, `npm run api:check:sim`, `git diff etc/statemachine-sim.api.md`, `node test/verify-dist-bytes.cjs` — each if: matrix.node-version==20, none on bun/node-18/tier-b. sim:pr runs a bounded safety+liveness budget + computeCoverage + threshold-free perf SMOKE + an ISS-030 async-action witness. (C) Core-bundle byte/hash guard wired into release.yml after verify-dist.cjs (release chain uses bun run, skips prepublishOnly) + already in prepublishOnly. (D) New sim-nightly.yml: cron '0 3 * * *' + workflow_dispatch, shard:[0..7] fail-fast:false, timeout-minutes:60, working-directory pinned, SM_SIM=1 SIM_SHARD=k SIM_SHARDS=8 npm run sim:sweep, upload-artifact@v4 if:failure() of packages/statemachine/.sim-out/**, perf regression (sim:perf) nightly-only. (E) .sim-out/** gitignored. New CLI: src/sim/cli/sim-pr.ts (prGateMain/runPrGate) + src/sim/cli/sim-sweep.ts (sweepMain/runSweep/shardSeeds/writeRepro) — registered as knip entries (NOT public barrel, so etc/statemachine-sim.api.md ZERO diff). Settle only via shared runScenario (R2); no Math.random/Date.now/flush/drain. Obligations: ISS-032 (nightly perf wiring), ISS-029 (node-20 coverage gate + positive string-method residual), ISS-033 (pins ungated), ISS-030 (sim:pr async witness).

FIX 1 (AC-8 sim:coverage exit 0): rewired sim:coverage from `vitest run --coverage src/tests/sim/coverage.test.ts` (which tripped vitest's global v8 90% line-threshold on core engine files) to a CLI-based ADR-8 capability-coverage gate. New CLI src/sim/cli/sim-coverage.ts exports runCoverageGate() wrapping computeCoverage(COVERAGE_SCENARIOS); new runner src/tests/sim/sim-coverage.run.test.ts (SM_SIM-gated, mirrors sim-pr.run.test.ts) asserts exitCode/uncovered/drift; package.json sim:coverage now `SM_SIM=1 vitest run src/tests/sim/sim-coverage.run.test.ts`; knip.json adds src/sim/cli/sim-coverage.ts! entry. sim:coverage now exits 0; negative proof (empty scenarios -> exitCode!=0, uncovered>0) verified then reverted. FIX 2 (AC-4 api.md): ran `npm run api:check` (api-extractor run --local) regenerating etc/statemachine.api.md in place to capture the already-shipped TASK-013 symbols (Clock, createVirtualScheduler, ITimerScheduler.process?, StateMachineOptions.clock?). api:check exits 0; diff contains ONLY those 4 symbols; precise whole-word sim-symbol scan of core api.md is clean (zero sim leak). Re-verify: npm run check GREEN; SM_SIM=1 vitest 776/0/59 (+1 test/+1 file = new runner); sim:coverage exit 0; zero core engine src files changed outside src/sim.

F-PF-1 (HIGH): restored ADR-8 mandatory coverage gate. DOCUMENTED_GAP_IDS (capabilities.ts) reduced 12->7 (frozen-minimal 4 + 3 amended structural gaps). 5 ids now COVERED via wire-time coverage-runner drive paths in coverage.ts (driveOverflowFrame/driveTransitionTimeoutFrame/drivePostRestoreFrame) keyed on new CoverageScenario envelope intents (overflow/transitionTimeout/snapshotRestore); scenarios added in scenarios/backpressure-timeout.ts + scenarios/persistence.ts (snapshotRestore). 3 kept gapped (queue.internal-before-external [empirically internal===0 at every boundary], event.onSuccess [never dispatched], event.onError [un-awaited, void]) with inline structural-unreachability comments + TECH_SPEC §3.7 amendment recorded (section 11 of tech-spec-sim-api artifact). driver.ts classifyError extended to recognize transition-timeout by phase:action+action+cause===undefined (gated on configured transitionTimeout; never reads e.message). Pin test capabilities_gap_pin.test.ts deep-equals the 7-id set + negative-proof (remove any covering scenario => id uncovered + exit 1). Gate: 32 covered/39, exit 0. F-PF-2 (HIGH): wired public Safety path. public.ts Simulator threads opts.invariants -> buildConfigGraph CheckerContext -> blind runSafety at each step() boundary; StepOutcome.violation = lowest-step so far; SimResult.violation = first/lowest-step, ok = (violation===undefined). repro-codegen.ts emitted test now replays via public runSimulation with frozen INVARIANTS registry (was invariants:[]), closing DoD-9b. Frozen public signatures unchanged (wired, not reshaped).

F-Q-1 (HIGH) QA-exit revision: added ONE ungated end-to-end test src/tests/sim/dst-e2e-repro.test.ts (4 tests) proving the UR-002 chain WHOLE: real generator (generateScenario(7n)) -> REAL engine via public runSimulation genuinely enters the parallel-region join composite 'cP.rA.fin|cP.rB.fin' -> consumer reachability Invariant (sanctioned SimOptions.invariants) fires a real Violation (ok:false, engine-computed witness) -> shrink via the real runScenario+runSafety surface (32 ops -> 5 ops, moves=4, minimal=true, same fingerprint) -> emit via real emitReproTest codegen -> RE-EXECUTE the emitted repro's replay through public runSimulation, re-failing the SAME {invariantId,witness} fingerprint. Fidelity option c (in-process replay through public ./sim); the literal emitted file's INVARIANTS-wired path is the codegen-shape contract documented in-test. No core engine src touched; no frozen public ./sim signature changed.

## Files Modified

- NEW src/sim/scenario.ts
- NEW src/sim/topology.ts
- NEW src/sim/ops.ts
- NEW src/sim/define.ts
- EDIT src/sim/index.ts (barrel: +Step-4 VALUE symbol re-exports)
- NEW src/tests/sim/generator.test.ts
- NEW src/tests/sim/replay.test.ts
- ZERO core engine files modified (git status confirms only src/sim/** + src/tests/sim/**); etc/statemachine.api.md zero git-diff.

NEW: src/sim/faults.ts, src/sim/harness.ts, src/sim/observable-scheduler.ts, src/tests/sim/faults.test.ts. EDITED (within src/sim, allowed): src/sim/index.ts (barrel re-exports), src/sim/scenario.ts (FaultPlan import from faults.ts, removed redundant local fault types). Verification: npx tsc --noEmit clean; npx vitest run src/tests/sim/ = 176 passed (10 files, incl 45 new faults.test.ts cases); npm run check (biome+tsc+knip) green. Core engine: ZERO tracked src/** file outside src/sim modified; etc/statemachine.api.md ZERO git-diff. knip.json entry change is a pre-existing prior-step working-tree artifact, not a Step-5 edit.

EDITED: .github/workflows/ci.yml (4 node-20-only DST steps), .github/workflows/release.yml (byte guard after verify-dist.cjs), .gitignore (.sim-out/), packages/statemachine/knip.json (cli entries), packages/statemachine/package.json (sim:pr/sim:sweep scripts). NEW: .github/workflows/sim-nightly.yml, packages/statemachine/src/sim/cli/sim-pr.ts, packages/statemachine/src/sim/cli/sim-sweep.ts, packages/statemachine/src/tests/sim/{sim-pr.run.test.ts,sim-sweep.run.test.ts,ci-gating.test.ts,sweep-shard.test.ts,repro-artifact.test.ts,perf-placement.test.ts}, plus AC-1 four-run canary added to existing replay.test.ts. NO core engine src touched. Verification: npm run check GREEN; default npm test 767 pass/8 skip; SM_SIM=1 sim suite 384 pass/0 skip; node test/verify-dist-bytes.cjs exits 0; api:check:sim ZERO diff.

## Key Decisions

ISS-043 RESOLVED = BRANCH B (documented coupling), the TECH_SPEC §6 recorded default. Rationale: emission half is feasible with zero new config (tsc -p tsconfig.build.json --emitDeclarationOnly emits types/sim/index.d.ts per-file, consumed by api-extractor.sim.json + the ./sim exports types key); typecheck isolation (branch A: tsconfig.check.json/tsconfig.sim.json excluding src/sim/**/*) was NOT taken because the extends-chain exclude-replacement + sim-leg core-.d.ts re-emit footguns outweigh the benefit for a byte-frozen-core island whose own 361-test sim suite gates every sim change. Accepted risk: a src/sim/** type error blocks core check/build/prepublishOnly/tier-a. REFLECT records sign-off. NOTE for REFLECT: dist-bytes.baseline.json toolchain is node v24.9.0 (this dev machine) — the node-20 CI-runner-pinned VALUE is a Step-11 CI measurement (SM_DIST_UPDATE_BASELINE=1 on the node-20 leg); the byte guard's rebuild-stability passes against the committed baseline here. Core etc/statemachine.api.md regeneration adds Clock/createVirtualScheduler/ITimerScheduler.process?/StateMachineOptions.clock? — these are ALREADY-MERGED TASK-013 core exports (present in src/index.ts at HEAD); the committed api.md was simply stale, NOT a sim-introduced ABI drift. No core engine src file was modified (dist/index.{js,cjs} byte-identical to baseline).

## Commit Trail

## UR Coverage

- [x] UR-002
