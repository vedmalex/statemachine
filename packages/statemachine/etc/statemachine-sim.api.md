## API Report File for "@vedmalex/statemachine"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public (undocumented)
export interface Adapter<T extends object> {
    // (undocumented)
    get adaptee(): T;
    // (undocumented)
    get(property: keyof T): T[keyof T];
    // (undocumented)
    set(property: keyof T, value: T[keyof T]): void;
}

// @public
export function analyzeLiveness(samples: readonly LivenessSample[], params: LivenessParams): LivenessResult;

// @public
export function applyQueueFaults(buffer: readonly SubmissionEntry[], plan: FaultPlan): SubmissionEntry[];

// @public
export function applyThrowFaults<T extends object>(config: StateMachineConfig<T>, plan: FaultPlan, env: Env, onInjected: (site: FaultSite) => void): {
    config: StateMachineConfig<T>;
    uncovered: UncoveredMarker[];
};

// @public
export interface AsyncCounter {
    // (undocumented)
    enterAsync(): void;
    // (undocumented)
    exitAsync(): void;
    // (undocumented)
    inFlightAsyncCount(): number;
}

// @public
export interface BandResult {
    readonly band: number;
    // (undocumented)
    readonly baseline: number;
    // (undocumented)
    readonly metric: 'throughput' | 'memory' | 'latencyP99' | 'traceLen';
    // (undocumented)
    readonly observed: number;
    // (undocumented)
    readonly status: 'pass' | 'fail' | 'advisory' | 'na';
}

// @public
export interface Bounds {
    readonly maxArmedDelay: number;
    readonly maxEventsCount: number;
    readonly maxOps: number;
    readonly maxStateDepth: number;
    readonly maxStatesCount: number;
}

// @public
export function bracketAsync<O>(env: Env, fn: WrappableAction<O>): WrappableAction<O>;

// @public
export function buildConfigGraph(config: unknown): ConfigGraph;

// @public
export function buildMinimalRepro(result: ShrinkResult, packageVersion: string): MinimalRepro;

// @public
export function buildOverflowFlood(entry: SubmissionEntry, floodCount: number): SubmissionEntry[];

// @public
export interface CanonicalHeader {
    readonly configHash: string;
    // (undocumented)
    readonly engine: string;
    readonly errorHandlerEnabled: true;
    // (undocumented)
    readonly prngVersion: typeof PRNG_VERSION;
    readonly runtime: string;
    readonly seed: string;
    readonly version: string;
}

// @public
export interface CanonicalTrace {
    // (undocumented)
    readonly frames: readonly TraceFrame[];
    // (undocumented)
    readonly header: CanonicalHeader;
}

// @public
export const CAPABILITIES: Record<CapabilityId, Capability>;

// @public
export interface Capability {
    readonly coverageStatus?: 'covered' | 'dormant' | 'n/a-string-method';
    readonly engineRefs: readonly string[];
    // (undocumented)
    readonly id: CapabilityId;
    // (undocumented)
    readonly probe: CapabilityProbe;
    // (undocumented)
    readonly tier?: 'core' | 'advanced';
    // (undocumented)
    readonly title: string;
}

// @public
export type CapabilityId = 'event.fire.external' | 'event.raise.internal' | 'queue.internal-before-external' | 'transition.guard.pass' | 'transition.guard.block' | 'transition.priority' | 'transition.onTransition' | 'event.wildcard' | 'hook.entry.onBeforeEnter' | 'hook.entry.onEnter' | 'hook.entry.onAfterEnter' | 'hook.exit.onBeforeExit' | 'hook.exit.onExit' | 'hook.exit.onAfterExit' | 'event.onBefore' | 'event.onAfter' | 'event.onSuccess' | 'event.onError' | 'hierarchy.nested-enter' | 'composite.parallel-regions' | 'composite.join.done-state' | 'history.shallow' | 'history.deep' | 'timer.invoke.fire' | 'timer.invoke.cond-skip' | 'timer.invoke.cancel-on-exit' | 'timer.transitionTimeout' | 'timer.resume' | 'error.action-throw' | 'error.guard-throw' | 'error.recovery.errorState' | 'error.recovery.abortOnExitError' | 'queue.backpressure.overflow' | 'queue.depth-bound.max-transition' | 'persistence.serialize' | 'persistence.deserialize' | 'inspection.getQueueDepth' | 'inspection.getCurrentStateInfo' | 'inspection.isDone';

// @public
export function capabilityKeys(): CapabilityId[];

// @public
export type CapabilityProbe = (trace: SimTrace) => boolean;

// @public
export interface CapturedWrite {
    readonly from: string;
    readonly to: string;
}

// @public
export function captureRejection(error: unknown, origin?: RejectionOrigin): ErrorClass | undefined;

// @public
export interface CaptureSink {
    // (undocumented)
    onStateWrite(write: CapturedWrite): void;
}

// @public
export interface CheckerContext {
    readonly frames?: readonly TraceFrame[];
    // (undocumented)
    readonly graph: ConfigGraph;
    // (undocumented)
    readonly header: CanonicalHeader;
    readonly lifecycle?: readonly LifecycleObservation[];
    readonly maxQueueDepth?: number;
    readonly raises?: readonly LifecycleObservation[];
    readonly raisesTruncated?: boolean;
}

// @public (undocumented)
export interface CheckEventSpec<T extends object> {
    // (undocumented)
    readonly name: string;
    readonly payload?: (rng: CheckRng, snapshot: MachineSnapshot<T>) => readonly unknown[];
    readonly weight?: number;
}

// @public (undocumented)
export function checkMachine<T extends object>(config: StateMachineConfig<T>, owner: OwnerSource<T>, options?: CheckOptions<T>): Promise<CheckReport>;

// @public
export type CheckMinimalOp = {
    readonly kind: 'fire';
    readonly event: string;
    readonly args?: readonly unknown[];
    readonly argsNote?: 'non-serializable';
} | {
    readonly kind: 'advance';
    readonly dtMs: number;
} | {
    readonly kind: 'noop';
};

// @public
export interface CheckMinimalRepro {
    readonly ops: readonly CheckMinimalOp[];
    // (undocumented)
    readonly provenance: {
        readonly runs: number;
        readonly moves: number;
        readonly minimal: boolean;
    };
    readonly trace: readonly TraceFrame[];
}

// @public (undocumented)
export interface CheckOptions<T extends object> {
    readonly degradationExcept?: readonly WarningKind[];
    // (undocumented)
    readonly events?: readonly CheckEventSpec<T>[];
    readonly failOn?: readonly FailCause[];
    // (undocumented)
    readonly invariants?: readonly MachineInvariant<T>[];
    readonly maxTurns?: number;
    // (undocumented)
    readonly mode?: 'safety' | 'liveness' | 'both';
    // (undocumented)
    readonly onRealTimerEscape?: 'warn' | 'fail' | 'ignore';
    // (undocumented)
    readonly runs?: number;
    readonly script?: readonly CheckScriptOp[];
    // (undocumented)
    readonly seed?: string | bigint;
    readonly shrink?: boolean | {
        readonly budget?: Partial<OpShrinkBudget>;
    };
    // (undocumented)
    readonly steps?: number;
}

// @public (undocumented)
export interface CheckReport {
    // (undocumented)
    readonly deadEvents: readonly string[];
    // (undocumented)
    readonly deadlocks: ReadonlyArray<{
        state: string;
    }>;
    readonly failedOn: readonly FailCause[];
    readonly guardOutcomes: ReadonlyArray<{
        transition: string;
        sawTrue: boolean;
        sawFalse: boolean;
    }>;
    // (undocumented)
    readonly livelocks: ReadonlyArray<{
        reason: string;
    }>;
    readonly nonConvergingRegions: ReadonlyArray<{
        composite: string;
        region: string;
    }>;
    // (undocumented)
    readonly ok: boolean;
    readonly oraclesRun: number;
    // (undocumented)
    readonly reachableStates: readonly string[];
    // (undocumented)
    readonly runs: number;
    readonly saturation: {
        readonly plateauedAtRun: number | null;
        readonly newCoveragePerRun: readonly number[];
    };
    // (undocumented)
    readonly seed: string;
    // (undocumented)
    readonly steps: number;
    readonly transitionsFired: number;
    // (undocumented)
    readonly uncoveredTransitions: ReadonlyArray<{
        event: string;
        from: string;
        to: string;
    }>;
    // (undocumented)
    readonly unreachableStates: readonly string[];
    // (undocumented)
    readonly violations: readonly CheckViolation[];
    // (undocumented)
    readonly warnings: readonly CheckWarning[];
}

// @public
export interface CheckRng {
    float(): number;
    int(max: number): number;
    pick<A>(xs: readonly A[]): A;
}

// @public
export type CheckScriptOp = ShrinkableOp;

// @public (undocumented)
export interface CheckViolation {
    // (undocumented)
    readonly invariant: string;
    readonly kind: 'engine' | 'builtin' | 'user';
    readonly minimal?: CheckMinimalRepro;
    readonly reproCode: string;
    // (undocumented)
    readonly witness: string;
}

// @public (undocumented)
export interface CheckWarning {
    // (undocumented)
    readonly detail: string;
    // (undocumented)
    readonly kind: WarningKind;
}

// @public
export function classifyCorruptState(probe: CorruptStateProbe): ErrorClass;

// @public
export function classifyError(e: unknown, origin?: RejectionOrigin): ErrorClass | undefined;

// @public
export function classifyQuiescence(sample: LivenessSample): Quiescence;

// @public
export type Clock = () => number;

// @public
export function computeCoverage(scenarios: readonly CoverageScenario[]): Promise<CoverageResult>;

// @public
export const condSkipScenario: CoverageScenario;

// @public
export interface ConfigGraph {
    // (undocumented)
    readonly composites: ReadonlySet<string>;
    readonly declaredDoneEvents: ReadonlySet<string>;
    depthOf(statePart: string): number;
    getRegionKey(statePart: string): string;
    isRegisteredLeaf(statePart: string): boolean;
    // (undocumented)
    readonly states: ReadonlySet<string>;
}

// @public
export function configHash(config: unknown): string;

// @public
export function configHasNoErrors(spec: ScenarioSpec): boolean;

// @public
export const coreEventsScenario: CoverageScenario;

// @public
export interface CorruptStateProbe {
    // (undocumented)
    readonly delivery: 'restore' | 'transition-target' | 'unregistered-leaf';
    // (undocumented)
    readonly expectedErrorClass: 'contradictory-state' | 'invalid-state-path';
    readonly expectedMessagePrefix: string;
    // (undocumented)
    readonly invariant: 'I-6' | 'I-10';
    // (undocumented)
    readonly kind: 'corrupt-state';
    readonly payload: string;
}

// @public
export const COVERAGE_RUNTIME = "node-sim-coverage-v1";

// @public
export const COVERAGE_SCENARIOS: readonly CoverageScenario[];

// @public
export class CoverageDeterminismError extends Error {
    constructor(seed: string, hashA: string, hashB: string);
    // (undocumented)
    readonly hashA: string;
    // (undocumented)
    readonly hashB: string;
    // (undocumented)
    readonly seed: string;
}

// @public
export function coverageMain(scenarios: readonly CoverageScenario[]): Promise<number>;

// @public
export interface CoverageResult {
    // (undocumented)
    readonly covered: ReadonlySet<CapabilityId>;
    // (undocumented)
    readonly drift: ReadonlySet<CapabilityId>;
    readonly exitCode: number;
    readonly status: ReadonlyMap<CapabilityId, 'covered' | 'dormant' | 'n/a-string-method'>;
    // (undocumented)
    readonly uncovered: ReadonlySet<CapabilityId>;
}

// @public
export interface CoverageScenario {
    readonly errorState?: string;
    // (undocumented)
    readonly expects?: readonly CapabilityId[];
    // (undocumented)
    readonly name: string;
    readonly overflow?: {
        readonly event: string;
        readonly maxQueueDepth: number;
        readonly floodCount: number;
    };
    readonly snapshotRestore?: boolean;
    // (undocumented)
    readonly spec: ScenarioSpec;
    readonly transitionTimeout?: {
        readonly event: string;
        readonly timeoutMs: number;
    };
}

// @public
export function createVirtualScheduler(clock: Clock): ITimerScheduler;

// @public
export const DEFAULT_BOUNDS: Bounds;

// @public
export const DEFAULT_GENOPS_PARAMS: GenOpsParams;

// @public
export const DEFAULT_MAX_TURNS = 1024;

// @public (undocumented)
export const DEFAULT_OP_SHRINK_BUDGET: OpShrinkBudget;

// @public
export const DEFAULT_SHRINK_BUDGET: ShrinkBudget;

// @public
export function defaultShrinkRunner(candidate: ScenarioSpec): Promise<ShrinkRunResult>;

// @public
export function defineScenario(spec: ScenarioSpec): ScenarioSpec;

// @public
export const DOCUMENTED_GAP_IDS: ReadonlySet<CapabilityId>;

// @public
export interface DriverConfig<T extends object> {
    readonly abortOnExitError?: boolean;
    // (undocumented)
    readonly clock: SimClock;
    // (undocumented)
    readonly config: StateMachineConfig<T>;
    // (undocumented)
    readonly errorHandler: SimErrorHandler;
    readonly errorState?: string;
    readonly faults?: FaultPlan;
    // (undocumented)
    readonly logger: ILogger;
    readonly maxQueueDepth?: number;
    readonly maxTurns?: number;
    // (undocumented)
    readonly monitor: SimMonitor;
    readonly onFrame?: (frame: TraceFrame) => void;
    readonly owner: Adapter<T>;
    readonly policy?: SettlePolicy;
    readonly prng: Prng;
    readonly runtime: string;
    readonly scheduler: {
        process(now?: number): void;
        isActive(): boolean;
        schedule(d: number, cb: () => void): object;
        cancel(t: object): void;
    };
    readonly schedulerView: SchedulerView;
    readonly transitionTimeout?: number;
}

// @public
export type DriverOp = {
    readonly kind: 'fire';
    readonly event: string;
    readonly args?: readonly unknown[];
    readonly opId?: string;
} | {
    readonly kind: 'advance';
    readonly dtMs: number;
    readonly opId?: string;
} | {
    readonly kind: 'noop';
    readonly opId?: string;
};

// @public
export interface DriverStepResult {
    // (undocumented)
    readonly frames: readonly TraceFrame[];
    // (undocumented)
    readonly quiescent: boolean;
    // (undocumented)
    readonly step: number;
    // (undocumented)
    readonly t: number;
}

// @public
export function emitRepro(result: ShrinkResult, packageVersion: string): {
    readonly record: MinimalRepro;
    readonly testSource: string;
};

// @public
export function emitReproTest(repro: MinimalRepro): string;

// @public
export const ENGINE_MESSAGE_FIXTURES: {
    readonly queueOverflow: "Event queue overflow — possible infinite loop";
    readonly maxTransitionDepth: "Max transition depth exceeded — possible infinite loop";
    readonly transitionTimeout: "Transition timeout";
    readonly invalidEventPrefix: "Invalid event:";
    readonly contradictoryStatePrefix: "Contradictory state detected";
    readonly invalidStatePathPrefix: "Invalid state path in current state";
};

// @public
export interface Env extends SchedulerView {
    enterAsync(): void;
    exitAsync(): void;
    inFlightAsyncCount(): number;
}

// @public
export type ErrorClass = 'queue-overflow' | 'max-transition-depth' | 'transition-timeout' | 'invalid-event' | 'injected-fault' | 'contradictory-state' | 'invalid-state-path';

// @public
export const errorsFaultsScenario: CoverageScenario;

// @public
export function evaluatePerfBands(report: PerfReport, baseline: PerfBaselineFile): BandResult[];

// @public
export interface EventSpec {
    // (undocumented)
    readonly onAfter?: LiteralCallback;
    readonly onBefore?: LiteralCallback;
    // (undocumented)
    readonly onSuccess?: LiteralCallback;
    // (undocumented)
    readonly transitions: readonly TransitionSpec[];
}

// @public
export type FailCause = 'violation' | 'deadlock' | 'non-converging' | 'no-progress' | 'livelock' | 'escape' | 'degradation';

// @public
export interface FaultCursor {
    faultStep: number;
}

// @public
export type FaultKind = 'reorder' | 'drop' | 'dup' | 'overflow' | 'clock-skew' | 'timer-jitter' | 'throw';

// @public
export interface FaultPlan {
    // (undocumented)
    readonly faults: readonly FaultSpec[];
    // (undocumented)
    readonly reorderWindow?: number;
    // (undocumented)
    readonly transitionTimeoutMs?: number;
}

// @public
export interface FaultRecord {
    // (undocumented)
    readonly faultStep: number;
    // (undocumented)
    readonly kind: FaultKind | 'corrupt-state';
    // (undocumented)
    readonly opId?: string;
    // (undocumented)
    readonly site: FaultSite;
}

// @public
export interface FaultSchedule {
    readonly budgetVirtualMs: number;
    readonly healAtVirtualMs: number;
    isHealthyAt(t: number): boolean;
}

// @public
export interface FaultSite {
    readonly armEpoch?: number;
    readonly callbackKind?: 'guard' | 'onTransition' | 'state.onBeforeEnter' | 'state.onEnter' | 'state.onAfterEnter' | 'state.onBeforeExit' | 'state.onExit' | 'state.onAfterExit' | 'event.onBefore' | 'event.onAfter' | 'event.onSuccess' | 'invoke.action';
    // (undocumented)
    readonly invokeIndex?: number;
    // (undocumented)
    readonly opId?: string;
    // (undocumented)
    readonly seam: 'event-queue' | 'scheduler' | 'callback';
    // (undocumented)
    readonly stateName?: string;
}

// @public
export type FaultSpec = {
    readonly kind: 'reorder' | 'drop' | 'dup';
    readonly site: FaultSite;
    readonly opId: string;
} | {
    readonly kind: 'overflow';
    readonly site: FaultSite;
    readonly opId: string;
    readonly floodCount: number;
} | {
    readonly kind: 'clock-skew';
    readonly site: FaultSite;
    readonly deltaMs: number;
} | {
    readonly kind: 'timer-jitter';
    readonly site: FaultSite;
    readonly jitterMs: number;
} | {
    readonly kind: 'throw';
    readonly site: FaultSite;
};

// @public
export interface FinalState {
    readonly config: string;
    // (undocumented)
    readonly queue: {
        readonly internal: number;
        readonly external: number;
    };
    // (undocumented)
    readonly quiescent: boolean;
}

// @public
export function finalStateOf(trace: CanonicalTrace): FinalState;

// @public
export function fingerprintEquals(a: ViolationFingerprint, b: ViolationFingerprint): boolean;

// @public
export function fingerprintOf(sample: LivenessSample): ProgressFingerprint;

// @public
export function fingerprintsEqual(a: ProgressFingerprint, b: ProgressFingerprint): boolean;

// @public
export function fireBuffered<T extends object>(fireEvent: (event: never, adapter: never, ...args: unknown[]) => Promise<boolean>, wrapped: Adapter<T>, entry: SubmissionEntry): Promise<FireResult>;

// @public
export type FireOutcome = 'resolve-true' | 'resolve-false' | 'reject';

// @public
export interface FireResult {
    // (undocumented)
    readonly errorClass?: ErrorClass;
    // (undocumented)
    readonly opId: string;
    // (undocumented)
    readonly outcome: FireOutcome;
    readonly syncAtEnqueue: boolean;
}

// @public
export const FNV64_OFFSET = 14695981039346656037n;

// @public
export const FNV64_PRIME = 1099511628211n;

// @public
export function formatCoverageReport(result: CoverageResult): string;

// @public
export function genConfig(prng: Prng, bounds: Bounds): GeneratedTopology;

// @public
export interface GeneratedTopology {
    // (undocumented)
    readonly topology: TopologySpec;
}

// @public
export function generateScenario(seed: bigint, bounds?: Bounds, params?: GenOpsParams): Promise<ScenarioSpec>;

// Warning: (ae-forgotten-export) The symbol "OpPrng" needs to be exported by the entry point index.d.ts
//
// @public
export function genOps(prng: OpPrng, driver: OpDriverView, params: GenOpsParams): Promise<Op[]>;

// @public
export interface GenOpsParams {
    readonly maxOps: number;
    readonly pAdvance: number;
    readonly pBlind: number;
}

// @public
export interface GenOwner {
    // (undocumented)
    k: number;
    // (undocumented)
    log: number[];
    // (undocumented)
    state: string;
}

// @public
export function hashTrace(trace: CanonicalTrace): string;

// @public
export const hierarchyRegionsScenario: CoverageScenario;

// @public
export const historyTimersScenario: CoverageScenario;

// @public
export const I10_PROBE: CorruptStateProbe;

// @public
export const I6_PROBE: CorruptStateProbe;

// @public
export interface IErrorHandler {
    // Warning: (ae-forgotten-export) The symbol "ErrorRecoveryStrategy" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    addRecoveryStrategy(strategy: ErrorRecoveryStrategy): void;
    // (undocumented)
    disable(): void;
    // (undocumented)
    enable(): void;
    // Warning: (ae-forgotten-export) The symbol "ErrorAnalytics" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    getAnalytics(): ErrorAnalytics;
    // (undocumented)
    isEnabled(): boolean;
    // (undocumented)
    removeRecoveryStrategy(strategyName: string): void;
}

// @public (undocumented)
export interface ILogger {
    // (undocumented)
    debug(message: string, context?: any): void;
    // (undocumented)
    error(message: string, context?: any, error?: Error): void;
    // (undocumented)
    info(message: string, context?: any): void;
    // (undocumented)
    warn(message: string, context?: any, error?: Error): void;
}

// @public
export interface IMonitor {
    // Warning: (ae-forgotten-export) The symbol "MonitorMetricsSnapshot" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    getMetrics?(): MonitorMetricsSnapshot;
    // Warning: (ae-forgotten-export) The symbol "ErrorContext" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    recordError(error: Error, context?: ErrorContext): void;
    // (undocumented)
    recordEvent?(eventName: string, duration: number): void;
    // Warning: (ae-forgotten-export) The symbol "LifecycleEvent" needs to be exported by the entry point index.d.ts
    recordLifecycle?(event: LifecycleEvent): void;
    // Warning: (ae-forgotten-export) The symbol "TransitionContext" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    recordTransition(duration: number, success: boolean, context?: TransitionContext): void;
}

// @public
export class InjectedFault extends Error {
    constructor(site?: FaultSite);
    readonly injectedFault: true;
    // (undocumented)
    readonly site?: FaultSite;
}

// @public
export interface Invariant {
    // (undocumented)
    readonly capabilityTags?: readonly string[];
    // (undocumented)
    checkFinal?(state: FinalState, ctx: CheckerContext): Violation | null;
    // (undocumented)
    checkStep?(frame: TraceFrame, ctx: CheckerContext): Violation | null;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly scope: InvariantScope;
}

// @public
export const INVARIANTS: readonly Invariant[];

// @public
export type InvariantScope = 'step' | 'final' | 'both';

// @public
export interface InvokeSpec {
    readonly action?: LiteralCallback;
    readonly cond?: LiteralCallback;
    // (undocumented)
    readonly delay: number;
    // (undocumented)
    readonly event: string;
}

// @public (undocumented)
export function isAdapter<T extends object>(inp: unknown): inp is Adapter<T>;

// @public
export function isGcExposed(): boolean;

// @public
export function isProgressBlocking(kind: FaultKind | 'corrupt-state'): boolean;

// @public
export function isStringMethodCallback(value: unknown): boolean;

// @public
export function issueCorruptStateWrite<T extends object>(wrapped: Adapter<T>, stateAttribute: keyof T, probe: CorruptStateProbe): string;

// @public
export interface ITimerScheduler {
    // (undocumented)
    cancel(token: object): void;
    // (undocumented)
    isActive(): boolean;
    process?(now?: number): void;
    // (undocumented)
    schedule(delay: number, callback: () => void): object;
}

// @public
export type JitterFn = (delay: number) => number;

// @public
export interface JitterRule {
    readonly jitterMs: number;
    readonly timerSiteId: string;
}

// @public
export type LatencyResolution = 'ms-coarse';

// @public
export interface LatencyStats {
    // (undocumented)
    readonly max: number;
    // (undocumented)
    readonly mean: number;
    // (undocumented)
    readonly p50: number;
    // (undocumented)
    readonly p90: number;
    // (undocumented)
    readonly p99: number;
    // (undocumented)
    readonly resolution: LatencyResolution;
}

// @public
export function latencyStatsOf(durations: readonly number[]): LatencyStats;

// @public
export interface LifecycleObservation {
    // (undocumented)
    readonly edge: 'begin' | 'end';
    // (undocumented)
    readonly event?: string;
    // (undocumented)
    readonly failed?: boolean;
    // (undocumented)
    readonly hook: string;
    // (undocumented)
    readonly kind: 'enter' | 'exit' | 'invoke' | 'guard' | 'raise';
    // (undocumented)
    readonly microstep: number;
    // (undocumented)
    readonly owner: object;
    // (undocumented)
    readonly seq: number;
    // (undocumented)
    readonly state: string;
    // (undocumented)
    readonly transition?: string;
}

// @public
export interface LiteralCallback {
    readonly fn: (...args: never[]) => unknown;
    readonly source: string;
}

// @public
export interface LivenessParams {
    readonly budgetVirtualMs: number;
    readonly stateCount: number;
}

// @public
export interface LivenessResult {
    // (undocumented)
    readonly quiescence: Quiescence;
    // (undocumented)
    readonly reason?: string;
    // (undocumented)
    readonly verdict: LivenessVerdict;
    // (undocumented)
    readonly witness?: string;
}

// @public
export interface LivenessSample {
    readonly config: string;
    readonly configChanged: boolean;
    // (undocumented)
    readonly earliestTimerAt: number | null;
    readonly fireOutcome?: 'resolve-true' | 'resolve-false' | 'reject';
    readonly healthy: boolean;
    readonly inFlight: boolean;
    // (undocumented)
    readonly pendingTimers: number;
    // (undocumented)
    readonly queueDepth: number;
    readonly t: number;
    readonly terminal: boolean;
    readonly waitingOnTransitionTimeout?: boolean;
}

// @public
export type LivenessVerdict = 'PROGRESSED' | 'STUCK' | 'TIMEOUT_BUDGET_EXCEEDED';

// @public
export function loadPerfBaseline(path: string): PerfBaselineFile;

// @public (undocumented)
export interface MachineInvariant<T extends object> {
    readonly check: (snapshot: MachineSnapshot<T>) => boolean;
    // (undocumented)
    readonly name: string;
}

// @public
export interface MachineSnapshot<T extends object> {
    readonly config: string;
    readonly data: Readonly<T>;
    // (undocumented)
    readonly queueDepth: number;
    readonly state: string;
}

// @public
export function makeAsyncCounter(): AsyncCounter;

// @public
export function makeEnv(counter: AsyncCounter, view: SchedulerView): Env;

// @public
export function makeFaultCursor(): FaultCursor;

// @public
export function makeFaultSchedule(opts: {
    healAtVirtualMs: number;
    budgetVirtualMs: number;
    longestArmedChainMs: number;
}): FaultSchedule;

// @public
export function makeObservableScheduler(clock: SimClock): ObservableSchedulerShim;

// @public
export function makeObservableSchedulerWithJitter(clock: SimClock, jitterFn?: JitterFn): ObservableSchedulerBundle;

// @public
export function makeOwner(topology: TopologySpec): GenOwner;

// @public
export function makePrng(seed: bigint | string): Prng;

// @public (undocumented)
export function makePrng(seed: number): Prng;

// @public
export function makeSimClock(start?: number): SimClock;

// @public
export function makeSiteKeyedJitter(prng: Prng, rules: readonly JitterRule[]): {
    enterSite(timerSiteId: string): void;
    exitSite(): void;
    jitterFn: JitterFn;
};

// @public
export function makeViolation(parts: {
    invariantId: string;
    step: number;
    witness: string;
    errorClass?: ErrorClass;
    message: string;
    observed: string;
    expected: string;
}): Violation;

// @public
export function markStringMethodUncovered(capability: UncoveredMarker['capability'], site: FaultSite): UncoveredMarker;

// @public
export const MASK64: bigint;

// @public
export function maybeWriteBaseline(path: string, report: PerfReport): boolean;

// @public
export function medianSample(samples: readonly PerfSample[]): PerfSample;

// @public
export const MINIMAL_REPRO_SCHEMA_VERSION = 1;

// @public
export interface MinimalRepro {
    // (undocumented)
    readonly packageVersion: string;
    // (undocumented)
    readonly provenance: {
        readonly shrinkMoves: number;
        readonly runs: number;
        readonly minimal: boolean;
    };
    // (undocumented)
    readonly replay: {
        readonly traceHash: string;
        readonly finalState: string;
        readonly finalQueueDepth: number;
    };
    // (undocumented)
    readonly scenario: ScenarioSpec;
    // (undocumented)
    readonly schemaVersion: number;
    // (undocumented)
    readonly seed: string;
    // (undocumented)
    readonly violation: {
        readonly invariantId: string;
        readonly witness: string;
        readonly errorClass?: ErrorClass;
        readonly step: number;
    };
}

// @public
export const NoopLogger: ILogger;

// @public
export function normalizeParts(s: string): string;

// @public
export interface ObservableScheduler extends SchedulerView {
    pendingCount(): number;
}

// @public
export interface ObservableSchedulerBundle {
    // (undocumented)
    readonly scheduler: ITimerScheduler;
    // (undocumented)
    readonly view: ObservableScheduler;
}

// @public
export interface ObservableSchedulerShim {
    // (undocumented)
    readonly scheduler: ITimerScheduler;
    // (undocumented)
    readonly view: SchedulerView;
}

// @public
export type Op = {
    readonly kind: 'fire';
    readonly id: string;
    readonly event: string;
    readonly args: readonly number[];
} | {
    readonly kind: 'advance';
    readonly id: string;
    readonly dtMs: number;
} | {
    readonly kind: 'noop';
    readonly id: string;
} | {
    readonly kind: 'snapshot';
    readonly id: string;
} | {
    readonly kind: 'restore';
    readonly id: string;
    readonly fromSnapshotId: string;
};

// @public
export interface OpDriverView {
    readonly machine: {
        getAvailableEvents(): string[];
        getQueueDepth(): {
            internal: number;
            external: number;
            total: number;
        };
    };
    step(op: {
        kind: 'fire';
        event: string;
        args?: readonly unknown[];
    } | {
        kind: 'advance';
        dtMs: number;
    } | {
        kind: 'noop';
    }): Promise<unknown>;
}

// @public (undocumented)
export interface OpShrinkBudget {
    readonly maxRuns: number;
    readonly maxStagnantRounds: number;
}

// @public
export type OpShrinkPredicate = (candidate: readonly ShrinkableOp[]) => Promise<boolean>;

// @public (undocumented)
export interface OpShrinkResult {
    readonly minimal: boolean;
    readonly moves: number;
    readonly ops: readonly ShrinkableOp[];
    readonly runs: number;
}

// @public
export const overflowScenario: CoverageScenario;

// @public
export type OwnerSource<T extends object> = T | Adapter<T> | (() => T | Adapter<T>);

// @public
export const PERF_BASELINE_PATH: string;

// @public
export const PERF_REGRESSION_CONFIG: {
    readonly medianN: 5;
    readonly bands: {
        readonly throughputPct: 0.2;
        readonly memoryPct: 0.25;
        readonly latencyP99Pct: 0.3;
        readonly traceLenTolerance: 0;
    };
    readonly p99Epsilon: 1e-9;
};

// @public
export const PERF_SEEDS: readonly bigint[];

// @public
export interface PerfBaselineFile {
    // (undocumented)
    readonly baseline: {
        readonly wallNs: string;
        readonly eventsPerSec: number;
        readonly transitionsPerSec: number;
        readonly latency: {
            readonly p50: number;
            readonly p90: number;
            readonly p99: number;
            readonly max: number;
            readonly mean: number;
            readonly resolution: 'ms-coarse';
        };
        readonly heapPeakBytes: number;
        readonly heapAvgBytes: number;
        readonly heapEndBytes: number;
        readonly gcProxy: number;
        readonly traceLen: number;
        readonly queueDepthPeak: number;
    };
    // (undocumented)
    readonly gates: {
        readonly latencyGated: boolean;
        readonly memoryGated: boolean;
    };
    // (undocumented)
    readonly node: string;
    // (undocumented)
    readonly packageVersion: string;
    // (undocumented)
    readonly runtime: 'node' | 'bun';
    // (undocumented)
    readonly schemaVersion: 1;
}

// @public
export class PerfBaselineValidationError extends Error {
    constructor(message: string);
}

// @public
export class PerfHarness {
    constructor(opts: RunPerfOptions);
    evaluate(baseline: PerfBaselineFile): Promise<{
        report: PerfReport;
        bands: BandResult[];
    }>;
    run(): Promise<PerfReport>;
}

// @public
export function perfMain(mode: 'gate' | 'update', baselinePath?: string): Promise<number>;

// @public
export interface PerfReport {
    // (undocumented)
    readonly node: string;
    // (undocumented)
    readonly packageVersion: string;
    readonly raw: PerfSample[];
    // (undocumented)
    readonly runtime: 'node' | 'bun';
    readonly sample: PerfSample;
    // (undocumented)
    readonly schemaVersion: 1;
}

// @public
export interface PerfSample {
    readonly eventsPerSec: number;
    // (undocumented)
    readonly eventsProcessed: number;
    // (undocumented)
    readonly gcProxy: number;
    // (undocumented)
    readonly heapAvgBytes: number;
    // (undocumented)
    readonly heapEndBytes: number;
    // (undocumented)
    readonly heapPeakBytes: number;
    readonly latency: LatencyStats;
    readonly queueDepthPeak: number;
    readonly traceLen: number;
    // (undocumented)
    readonly transitionsObserved: number;
    // (undocumented)
    readonly transitionsPerSec: number;
    readonly wallNs: bigint;
}

// @public
export const persistenceScenario: CoverageScenario;

// @public
export const PERTURB_ONLY_FAULTS: ReadonlySet<FaultKind>;

// @public
export function pinOps(ops: readonly Op[]): readonly Op[];

// @public
export function pinTopology(topology: TopologySpec): TopologySpec;

// @public
export interface Prng {
    bool(p?: number): boolean;
    fork(label: string): Prng;
    int(maxExclusive: number): number;
    nextFloat(): number;
    nextU32(): number;
    pick<T>(xs: readonly T[]): T;
    readonly seed: bigint;
    state(): bigint;
    weighted<T>(xs: readonly (readonly [T, number])[]): T;
}

// @public
export const PRNG_VERSION: "splitmix64-bigint-v1";

// @public
export const PROGRESS_BLOCKING_FAULTS: ReadonlySet<FaultKind | 'corrupt-state'>;

// @public
export const PROGRESS_RECENCY_WINDOW: number;

// @public
export interface ProgressFingerprint {
    readonly config: string;
    // (undocumented)
    readonly earliestTimerAt: number | null;
    // (undocumented)
    readonly pendingTimers: number;
    // (undocumented)
    readonly queueDepth: number;
}

// @public
export function progressRecencyWindow(maxTurns: number): number;

// @public
export const PUBLIC_PACKAGE = "@vedmalex/statemachine";

// @public (undocumented)
export const PUBLIC_SIM_ENTRY = "@vedmalex/statemachine/sim";

// @public
export type Quiescence = 'TERMINAL_FINAL' | 'QUIESCENT_NO_WORK' | 'WAITING_ON_TIMER' | 'WAITING_ON_TRANSITION_TIMEOUT' | 'ACTIVE';

// @public
export interface RejectionOrigin {
    readonly syncAtEnqueue: boolean;
}

// @public
export function rekeyFaultPlan(plan: FaultPlan, survivingOpIds: ReadonlySet<string>): FaultPlan;

// @public
export function resolveFaultAt(plan: FaultPlan, opId: string, cursor: FaultCursor, records: FaultRecord[]): FaultSpec | undefined;

// @public
export function runPerf(opts: RunPerfOptions): Promise<PerfReport>;

// @public
export interface RunPerfOptions {
    readonly bounds?: Bounds;
    readonly medianN?: number;
    readonly seeds: readonly bigint[];
}

// @public
export function runSafety(invariants: readonly Invariant[], trace: CanonicalTrace, ctx: CheckerContext): Violation | null;

// @public
export function runSafetyWithDeterminism(invariants: readonly Invariant[], trace: CanonicalTrace, ctx: CheckerContext, hashes: {
    readonly a: string;
    readonly b: string;
}): Violation | null;

// @public
export function runScenario(spec: ScenarioSpec): Promise<CanonicalTrace>;

// @public
export function runSimulation<T extends object>(setup: SimSetup<T>, opts: SimOptions): Promise<SimResult>;

// @public
export const SCENARIO_RUNTIME = "node-sim-v1";

// @public
export interface ScenarioSpec {
    // (undocumented)
    readonly bounds: Bounds;
    // (undocumented)
    readonly faults: FaultPlan;
    // (undocumented)
    readonly ops: readonly Op[];
    readonly seed: string;
    readonly topology: TopologySpec;
    // (undocumented)
    readonly version: 1;
}

// @public
export interface SchedulerView {
    earliestExecuteAt(): number | null;
    schedulerEmptyAt(t: number): boolean;
}

// @public
export interface SettleArgs {
    // (undocumented)
    readonly clock: SimClock;
    // (undocumented)
    readonly env: Env;
    readonly maxTurns?: number;
    readonly onClockJump?: (to: number) => void;
    // (undocumented)
    readonly policy: SettlePolicy;
    // (undocumented)
    readonly scheduler: SettleScheduler;
    // (undocumented)
    readonly sm: SettleTarget;
}

// @public
export function settleMacrostep(args: SettleArgs): Promise<SettleResult>;

// @public
export type SettlePolicy = 'safety' | 'liveness';

// @public
export type SettleReason = 'microtask-budget' | 'budget-progressing' | 'WAITING_ON_TIMER' | 'WAITING_ON_TRANSITION_TIMEOUT' | 'WAITING_ON_INTERNAL';

// @public
export interface SettleResult {
    readonly quiescent: boolean;
    readonly reason?: SettleReason;
    readonly t: number;
    readonly turns: number;
}

// @public
export interface SettleScheduler {
    process(now?: number): void;
}

// @public
export interface SettleTarget {
    // (undocumented)
    getQueueDepth(): {
        internal: number;
        external: number;
        total: number;
    };
    // (undocumented)
    isProcessingEvents(): boolean;
}

// @public
export function shrink(spec: ScenarioSpec, target: Violation, opts?: {
    readonly runner?: ShrinkRunner;
    readonly budget?: ShrinkBudget;
    readonly nowMs?: () => number;
}): Promise<ShrinkResult>;

// @public
export type ShrinkableOp = {
    readonly kind: 'fire';
    readonly event: string;
    readonly args?: readonly unknown[];
    readonly opId?: string;
} | {
    readonly kind: 'advance';
    readonly dtMs: number;
    readonly opId?: string;
} | {
    readonly kind: 'noop';
    readonly opId?: string;
};

// @public
export interface ShrinkBudget {
    readonly maxRuns: number;
    readonly maxStagnantRounds: number;
    readonly maxWallMs: number;
}

// @public
export function shrinkCacheKey(spec: ScenarioSpec): string;

// @public
export function shrinkOps(ops: readonly ShrinkableOp[], predicate: OpShrinkPredicate, budget?: OpShrinkBudget): Promise<OpShrinkResult>;

// @public
export interface ShrinkResult {
    readonly provenance: {
        readonly shrinkMoves: number;
        readonly runs: number;
        readonly minimal: boolean;
    };
    readonly run: ShrinkRunResult;
    readonly scenario: ScenarioSpec;
    readonly violation: Violation;
}

// @public
export type ShrinkRunner = (candidate: ScenarioSpec) => Promise<ShrinkRunResult>;

// @public
export interface ShrinkRunResult {
    readonly finalQueueDepth: number;
    readonly finalState: string;
    readonly traceHash: string;
    readonly violation: Violation | null;
}

// @public
export interface SimClock {
    now(): number;
    set(t: number): void;
    readonly t: number;
}

// @public
export class SimDriver<T extends object> {
    constructor(cfg: DriverConfig<T>);
    get environment(): Env;
    faultRecordsList(): readonly FaultRecord[];
    fireMany(ops: ReadonlyArray<{
        event: string;
        args?: readonly unknown[];
        opId: string;
    }>): Promise<readonly FireResult[]>;
    init(): Promise<void>;
    get machine(): StateMachine<T, StateMachineConfig<T>>;
    step(op: DriverOp): Promise<DriverStepResult>;
    get t(): number;
    trace(): CanonicalTrace;
}

// @public (undocumented)
export interface SimEnv {
    readonly clock: Clock;
    readonly errorHandler: IErrorHandler;
    readonly logger: ILogger;
    readonly monitor: IMonitor;
    now(): number;
    random(): number;
    readonly scheduler: ITimerScheduler;
}

// @public
export class SimError extends Error {
    constructor(message: string);
}

// @public
export class SimErrorHandler implements IErrorHandler {
    constructor();
    // (undocumented)
    addRecoveryStrategy(_strategy: ErrorRecoveryStrategy): void;
    // (undocumented)
    disable(): void;
    // (undocumented)
    enable(): void;
    // (undocumented)
    getAnalytics(): ErrorAnalytics;
    // (undocumented)
    isEnabled(): boolean;
    // (undocumented)
    removeRecoveryStrategy(_strategyName: string): void;
}

// @public
export type SimEventPayload = (event: string, rng: SimPayloadRng, snapshot: SimPayloadSnapshot) => readonly unknown[];

// @public
export class SimMonitor implements IMonitor {
    getDurations(): readonly number[];
    getErrorCount(): number;
    getFailureCount(): number;
    getLifecycle(): readonly LifecycleObservation[];
    getRaises(): readonly LifecycleObservation[];
    getTransitionContexts(): readonly TransitionContext[];
    getTransitionCount(): number;
    invokeActionInFlightCount(): number;
    isLifecycleTruncated(): boolean;
    isRaisesTruncated(): boolean;
    // (undocumented)
    recordError(_error: Error, _context?: ErrorContext): void;
    recordLifecycle(event: LifecycleEvent): void;
    // (undocumented)
    recordTransition(duration: number, success: boolean, context?: TransitionContext): void;
}

// @public (undocumented)
export interface SimOptions {
    readonly eventPayload?: SimEventPayload;
    // (undocumented)
    readonly faults?: FaultPlan;
    // (undocumented)
    readonly invariants?: readonly Invariant[];
    readonly maxQueueDepth?: number;
    readonly maxTurns?: number;
    readonly mode?: 'safety' | 'liveness' | 'both';
    // (undocumented)
    readonly onTrace?: (frame: TraceFrame) => void;
    readonly script?: readonly DriverOp[];
    // (undocumented)
    readonly seed: bigint | string;
    // (undocumented)
    readonly steps?: number;
}

// @public
export interface SimPayloadRng {
    float(): number;
    int(max: number): number;
    pick<A>(xs: readonly A[]): A;
}

// @public
export interface SimPayloadSnapshot {
    readonly config: string;
    readonly data: Readonly<object>;
    // (undocumented)
    readonly queueDepth: number;
    readonly state: string;
}

// @public (undocumented)
export interface SimResult {
    readonly livelocks?: readonly LivenessResult[];
    readonly liveness?: LivenessResult;
    // (undocumented)
    readonly metrics: PerfSample;
    // (undocumented)
    readonly ok: boolean;
    readonly ops?: readonly DriverOp[];
    readonly oraclesRun: number;
    // (undocumented)
    readonly seed: string;
    // (undocumented)
    readonly steps: number;
    // (undocumented)
    readonly trace: readonly TraceFrame[];
    // (undocumented)
    readonly traceHash: string;
    // (undocumented)
    readonly violation?: SimViolation;
    readonly warnings?: readonly SimWarning[];
}

// @public (undocumented)
export type SimSetup<T extends object = object> = (env: SimEnv) => SimTarget<T> | Promise<SimTarget<T>>;

// @public (undocumented)
export interface SimSnapshot {
    // (undocumented)
    readonly machine: string;
    // (undocumented)
    readonly prngState: string;
    // (undocumented)
    readonly seed: string;
    // (undocumented)
    readonly step: number;
    // (undocumented)
    readonly t: number;
}

// @public (undocumented)
export type SimTarget<T extends object = object> = {
    readonly config: StateMachineConfig<T>;
    readonly owner: T | Adapter<T>;
} | {
    readonly machine: StateMachine<T, StateMachineConfig<T>>;
};

// @public
export type SimTrace = CanonicalTrace;

// @public
export class Simulator<T extends object = object> {
    constructor(setup: SimSetup<T>, opts: SimOptions);
    get env(): SimEnv;
    init(): Promise<void>;
    run(): Promise<SimResult>;
    snapshot(): SimSnapshot;
    step(): Promise<StepOutcome>;
}

// @public (undocumented)
export type SimViolation = Violation & {
    readonly kind?: 'engine' | 'liveness';
};

// @public (undocumented)
export interface SimWarning {
    // (undocumented)
    readonly count?: number;
    // (undocumented)
    readonly kind: 'timer-escape' | 'unhandled-rejection' | 'no-oracles' | 'lifecycle-truncated'
    /**
    * The HARNESS ran out of per-macrostep turn budget while the machine was
    * still observably working (`settleReason:'budget-progressing'`). Advisory
    * only — it never flips `ok`, because the machine did nothing wrong: the
    * run simply did not get to watch it finish. Emitted at most ONCE per run;
    * `count` carries how many boundaries hit it.
    */
    | 'budget-progressing'
    /**
    * The HARNESS ran out of per-macrostep turn budget and the machine had
    * ALREADY stopped moving before it did (`settleReason:'microtask-budget'`).
    *
    * Advisory only, and for the same reason as its sibling: this is a TRUNCATED
    * observation. A genuine wedge and a hook doing a large but finite amount of
    * internal microtask work leave byte-identical prefixes — an awaited
    * `onEnter`/`onExit` is not counted in `inFlightAsyncCount`, so while one runs
    * the settle fingerprint is frozen either way. Raising `maxTurns` separates
    * them: a finite hook eventually completes. Emitted at most ONCE per run;
    * `count` carries how many boundaries hit it.
    */
    | 'budget-frozen'
    /**
    * A settle stopped at the pump's EARLY break with pending work, an armed
    * future timer, and nothing the harness can observe in flight
    * (`settleReason:'WAITING_ON_INTERNAL'`).
    *
    * Advisory only, and for exactly the same reason as the two budget kinds: it
    * is a TRUNCATED observation, just over a 16-turn window instead of a 1024-turn
    * one. The settle fingerprint (`queueDepth|isProcessingEvents|inFlightAsyncCount`)
    * freezes across an ENTIRE ordinary microstep — the engine awaits once per hook
    * slot per state even where no hook is defined — so one LEGITIMATE microstep's
    * frozen-turn count grows with the machine's own width. Measured: a parallel
    * composite whose only extra ingredient is an unrelated sibling region holding
    * `invoke: [{event:'never', delay:100000}]` reports this for a SYNCHRONOUS
    * `onEnter`, and for an async one awaiting 1, 3, 5 or 20 microtasks; without
    * that sibling timer the identical machine is clean.
    *
    * What it is still worth reading: it names the ONE macrostep where the drain
    * stopped moving while a deadline was armed. If a machine reports it at every
    * `steps`/`maxTurns` you try AND never advances past that configuration, that is
    * the place to look. Emitted at most ONCE per run; `count` carries how many
    * SETTLES hit it.
    */
    | 'rtc-unobserved';
    // (undocumented)
    readonly message: string;
}

// @public
export const SPLITMIX64_INCREMENT = 11400714819323198485n;

// @public
export const SPLITMIX64_MUL_1 = 13787848793156543929n;

// @public
export const SPLITMIX64_MUL_2 = 10723151780598845931n;

// @public (undocumented)
export class StateMachine<TOwner extends object, SMConfig extends StateMachineConfig<TOwner>> {
    // Warning: (ae-forgotten-export) The symbol "PropertiesOf" needs to be exported by the entry point index.d.ts
    constructor(config: SMConfig, adaptee?: Adapter<PropertiesOf<TOwner>> | PropertiesOf<TOwner>, options?: StateMachineOptions);
    // (undocumented)
    attachToObject(object: any, eventMap: {
        [key: string]: string;
    }): void;
    canFireEvent(eventName: keyof SMConfig['events'] | '*', adaptee?: Adapter<PropertiesOf<TOwner>>): boolean;
    canFireEventFor(owner: PropertiesOf<TOwner> | Adapter<PropertiesOf<TOwner>>, eventName: keyof SMConfig['events'] | '*'): boolean;
    // Warning: (ae-forgotten-export) The symbol "ContextTrackerKind" needs to be exported by the entry point index.d.ts
    get contextTrackerKind(): ContextTrackerKind;
    // Warning: (ae-forgotten-export) The symbol "StateName" needs to be exported by the entry point index.d.ts
    set currentState(state: StateName);
    // (undocumented)
    get currentState(): string;
    // (undocumented)
    fireEvent(eventName: keyof SMConfig['events'] | '*', ...args: any[]): Promise<boolean>;
    fireEventDetailed(eventName: keyof SMConfig['events'] | '*', ...args: any[]): Promise<FireResult_2>;
    // Warning: (ae-forgotten-export) The symbol "FireResult_2" needs to be exported by the entry point index.d.ts
    fireEventDetailedFor(owner: PropertiesOf<TOwner> | Adapter<PropertiesOf<TOwner>>, eventName: keyof SMConfig['events'] | '*', ...args: unknown[]): Promise<FireResult_2>;
    fireEventFor(owner: PropertiesOf<TOwner> | Adapter<PropertiesOf<TOwner>>, eventName: keyof SMConfig['events'] | '*', ...args: unknown[]): Promise<boolean>;
    // (undocumented)
    static fromData<TOwner extends object, SMConfig extends StateMachineConfig<TOwner>>(config: SMConfig, initialState?: string, context?: TOwner, options?: StateMachineOptions): StateMachine<TOwner, SMConfig>;
    // (undocumented)
    static fromJSON<TOwner extends object, SMConfig extends StateMachineConfig<TOwner>>(jsonData: string, obj?: TOwner | Adapter<TOwner>, options?: StateMachineOptions): StateMachine<TOwner, SMConfig>;
    // (undocumented)
    static fromJSONWithContext<TOwner extends object, SMConfig extends StateMachineConfig<TOwner>>(jsonData: string, context?: MethodsOf<TOwner>, options?: StateMachineOptions): StateMachine<TOwner, SMConfig>;
    static fromSecureJSON<TOwner extends object, SMConfig extends StateMachineConfig<TOwner>>(jsonData: string, obj?: TOwner | Adapter<TOwner>, options?: StateMachineOptions): Promise<StateMachine<TOwner, SMConfig>>;
    // (undocumented)
    getAvailableEvents(adaptee?: Adapter<PropertiesOf<TOwner>>): string[];
    getAvailableEventsFor(owner: PropertiesOf<TOwner> | Adapter<PropertiesOf<TOwner>>): string[];
    // Warning: (ae-forgotten-export) The symbol "CompiledModel" needs to be exported by the entry point index.d.ts
    //
    // @internal
    getCompiledModel(): CompiledModel;
    // (undocumented)
    getCurrentState(adaptee?: Adapter<PropertiesOf<TOwner>>): string | undefined;
    // Warning: (ae-forgotten-export) The symbol "StateInfo" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    getCurrentStateInfo(): StateInfo | undefined;
    getMetrics(): MonitorMetricsSnapshot | undefined;
    getMonitor(): IMonitor;
    // (undocumented)
    getQueueDepth(): {
        internal: number;
        external: number;
        total: number;
    };
    // Warning: (ae-forgotten-export) The symbol "QueuedEventInfo" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    getQueuedEvents(): QueuedEventInfo[];
    // (undocumented)
    getStateHistory(): Record<string, string>;
    isDone(compositeId: string, adaptee?: Adapter<PropertiesOf<TOwner>>): boolean;
    isInState(expectedState: StateName, adaptee?: Adapter<PropertiesOf<TOwner>>): boolean;
    // (undocumented)
    isProcessingEvents(): boolean;
    // (undocumented)
    get isTransitioning(): boolean;
    // (undocumented)
    reset(adaptee?: Adapter<PropertiesOf<TOwner>>): Promise<void>;
    // (undocumented)
    restoreState(adapter?: StatePersistenceAdapter): Promise<void>;
    // Warning: (ae-forgotten-export) The symbol "StatePersistenceAdapter" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    saveState(adapter?: StatePersistenceAdapter): Promise<void>;
    // Warning: (ae-forgotten-export) The symbol "MethodsOf" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    setContext(context: MethodsOf<TOwner>): void;
    // (undocumented)
    get targetState(): string | undefined;
    // (undocumented)
    toJSON(): string;
    toSecureJSON(): Promise<string>;
}

// @public (undocumented)
export interface StateMachineConfig<T extends object = object> {
    // (undocumented)
    description?: string;
    // Warning: (ae-forgotten-export) The symbol "EventName" needs to be exported by the entry point index.d.ts
    // Warning: (ae-forgotten-export) The symbol "Event_2" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    events: Record<EventName, Omit<Event_2<T, StateMachineConfig<T>['states']>, 'name'>>;
    // (undocumented)
    initialState: keyof StateMachineConfig<T>['states'];
    // (undocumented)
    name: string;
    // Warning: (ae-forgotten-export) The symbol "ErrorHandlerOrString" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    onError?: ErrorHandlerOrString<T>;
    // Warning: (ae-forgotten-export) The symbol "KeysOf" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    stateAttribute: KeysOf<PropertiesOf<T>, string>;
    // Warning: (ae-forgotten-export) The symbol "States" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    states: States<T>;
}

// @public (undocumented)
export interface StateMachineOptions {
    abortOnExitError?: boolean;
    // Warning: (ae-forgotten-export) The symbol "FunctionRegistry" needs to be exported by the entry point index.d.ts
    actions?: FunctionRegistry;
    clock?: () => number;
    // Warning: (ae-forgotten-export) The symbol "IContextTracker" needs to be exported by the entry point index.d.ts
    contextTracker?: IContextTracker;
    // (undocumented)
    errorHandler?: IErrorHandler;
    errorState?: string;
    // (undocumented)
    logger?: ILogger;
    maxQueueDepth?: number;
    maxTransitionDepth?: number;
    // (undocumented)
    monitor?: IMonitor;
    // (undocumented)
    scheduler?: ITimerScheduler;
    strictActions?: boolean;
    transitionTimeout?: number;
}

// @public
export interface StateSpec {
    readonly final?: boolean;
    // (undocumented)
    readonly history?: 'shallow' | 'deep';
    readonly initial?: string;
    // (undocumented)
    readonly invoke?: readonly InvokeSpec[];
    // (undocumented)
    readonly onAfterEnter?: LiteralCallback;
    // (undocumented)
    readonly onAfterExit?: LiteralCallback;
    readonly onBeforeEnter?: LiteralCallback;
    // (undocumented)
    readonly onBeforeExit?: LiteralCallback;
    readonly onEnter?: LiteralCallback;
    // (undocumented)
    readonly onExit?: LiteralCallback;
    readonly regions?: Readonly<Record<string, Readonly<Record<string, StateSpec>>>>;
    readonly states?: Readonly<Record<string, StateSpec>>;
}

// @public (undocumented)
export interface StepOutcome {
    // (undocumented)
    readonly done: boolean;
    // (undocumented)
    readonly frames: readonly TraceFrame[];
    // (undocumented)
    readonly quiescent: boolean;
    // (undocumented)
    readonly step: number;
    // (undocumented)
    readonly t: number;
    // (undocumented)
    readonly traceHash: string;
    // (undocumented)
    readonly violation?: Violation;
}

// @public
export interface SubmissionEntry {
    readonly args: readonly unknown[];
    // (undocumented)
    readonly event: string;
    readonly opId: string;
}

// @public
export function suppressesStuck(schedule: FaultSchedule, t: number): boolean;

// @public
export function timerSiteId(stateName: string, invokeIndex: number, armEpoch: number): string;

// @public
export function toBaselineFile(report: PerfReport): PerfBaselineFile;

// @public
export function toEngineConfig<T extends object = {
    state: string;
    log: number[];
    k: number;
}>(topology: TopologySpec): StateMachineConfig<T>;

// @public
export interface TopologySpec {
    // (undocumented)
    readonly events: Readonly<Record<string, EventSpec>>;
    // (undocumented)
    readonly initialState: string;
    // (undocumented)
    readonly name: string;
    readonly ownerSeed: Readonly<Record<string, unknown>>;
    // (undocumented)
    readonly stateAttribute: string;
    // (undocumented)
    readonly states: Readonly<Record<string, StateSpec>>;
}

// @public
export type TraceCause = 'init' | 'external' | 'timer' | 'internal';

// @public
export interface TraceFrame {
    // (undocumented)
    readonly cause: TraceCause;
    readonly doneDelta?: ReadonlyArray<{
        readonly composite: string;
        readonly done: boolean;
    }>;
    readonly errorClass?: ErrorClass;
    readonly event?: string;
    readonly faultApplied?: FaultKind;
    // (undocumented)
    readonly fireOutcome?: FireOutcome;
    readonly from: string;
    // (undocumented)
    readonly queue: {
        readonly internal: number;
        readonly external: number;
    };
    // (undocumented)
    readonly quiescent: boolean;
    readonly settleReason?: SettleReason;
    // (undocumented)
    readonly step: number;
    // (undocumented)
    readonly synthetic?: TraceSynthetic;
    readonly t: number;
    readonly to: string;
}

// @public
export type TraceSynthetic = 'errorState-fallback' | 'corrupt-state' | 'post-restore';

// @public
export interface TransitionSpec {
    // (undocumented)
    readonly from: string;
    // (undocumented)
    readonly guard?: LiteralCallback;
    // (undocumented)
    readonly onTransition?: LiteralCallback;
    // (undocumented)
    readonly priority?: number;
    // (undocumented)
    readonly to: string;
}

// @public
export const transitionTimeoutScenario: CoverageScenario;

// @public
export interface UncoveredMarker {
    readonly capability: 'error.guard-throw' | 'error.action-throw' | 'error.recovery.abortOnExitError';
    // (undocumented)
    readonly reason: 'string-method';
    // (undocumented)
    readonly site: FaultSite;
}

// @public
export function validatePerfBaseline(parsed: PerfBaselineFile, source?: string): PerfBaselineFile;

// @public
export interface Violation {
    // (undocumented)
    readonly errorClass?: ErrorClass;
    // (undocumented)
    readonly expected: string;
    readonly fingerprint: {
        readonly invariantId: string;
        readonly witness: string;
        readonly errorClass?: ErrorClass;
    };
    // (undocumented)
    readonly invariantId: string;
    readonly message: string;
    // (undocumented)
    readonly observed: string;
    // (undocumented)
    readonly step: number;
    readonly witness: string;
}

// @public
export type ViolationFingerprint = Violation['fingerprint'];

// @public (undocumented)
export type WarningKind = 'no-payload' | 'timer-escape' | 'dead-events-at-plateau' | 'uncovered-at-plateau' | 'residual-rejection'
/** A guard that never returned `true` across a SATURATED sweep (advisory). */
| 'dead-guard-at-plateau'
/** Detail carrier for {@link CheckReport.nonConvergingRegions}. */
| 'non-converging-region'
/** The initial-configuration invariant pass could not run — see the detail. */
| 'init-check-skipped'
/**
* Minimization was DECLINED for the reported finding — see the detail for
* which precondition failed. Its presence is the honest alternative to an
* UNVERIFIED `minimal`: the violation stands, only the 1-minimal repro is
* missing. Advisory (never a {@link FailCause}).
*/
| 'shrink-skipped'
/**
* The harness exhausted its per-macrostep TURN budget while the machine was
* still observably working (`settleReason:'budget-progressing'`). The machine
* did nothing wrong — the run just stopped watching before it settled, so the
* remainder of that macrostep went unchecked. The drain resumes on the next
* step, so raise `steps` (or {@link CheckOptions.maxTurns}) to give the machine
* more budget to finish in.
* Advisory (never a {@link FailCause}, never flips `ok`).
*/
| 'budget-progressing'
/**
* The harness exhausted its per-macrostep TURN budget and the machine had
* ALREADY stopped moving before it did (`settleReason:'microtask-budget'`).
*
* Also advisory, and deliberately so: the observation is TRUNCATED. A genuine
* wedge and an `onEnter`/`onExit` hook doing a large but finite amount of
* internal work leave the same frozen prefix, because an awaited hook is not
* counted as in-flight async. Raise {@link CheckOptions.maxTurns} to tell them
* apart — a finite hook eventually completes.
* Advisory (never a {@link FailCause}, never flips `ok`).
*/
| 'budget-frozen'
/**
* A macrostep stopped with work pending and a future timer armed while nothing
* the harness can observe was in flight (`settleReason:'WAITING_ON_INTERNAL'`).
*
* Advisory for the same reason as the two budget kinds — it is a truncated
* observation over a 16-turn window. The settle fingerprint is frozen for a whole
* ordinary microstep (the engine awaits once per hook slot per state even where no
* hook is defined), so a legitimate microstep freezes it for a number of turns
* that grows with the machine's width, against a fixed break window. Read it as a
* pointer to the macrostep worth inspecting, not a verdict.
* Advisory (never a {@link FailCause}, never flips `ok`).
*/
| 'rtc-unobserved'
/**
* An OBSERVATION BUFFER hit its cap and stopped retaining records, so a
* lifecycle-keyed oracle saw only a PREFIX of the run
* ({@link SimWarning} `lifecycle-truncated`).
*
* Carried through under its OWN kind rather than the `residual-rejection`
* catch-all: a truncated observation plane is a statement about how much of the
* run was CHECKED, and naming it "residual rejection" would report a completely
* different and much more alarming condition than the one observed — the exact
* laundering already fixed for the two budget kinds.
* Advisory (never a {@link FailCause}, never flips `ok`).
*/
| 'lifecycle-truncated';

// @public
export function wire<T extends object>(env: SimEnv, config: StateMachineConfig<T>, owner: T | Adapter<T>): StateMachine<T, StateMachineConfig<T>>;

// @public
export function wrapAdapterForCapture<T extends object>(adaptee: Adapter<T>, stateAttribute: keyof T, sink: CaptureSink): Adapter<T>;

// @public
export type WrappableAction<O> = (owner: O, ...args: unknown[]) => unknown | Promise<unknown>;

// @public
export function wrapThrowingCallback<O>(env: Env, fn: (owner: O, ...args: unknown[]) => unknown, site: FaultSite, shouldThrow: () => boolean, onInjected: (site: FaultSite) => void): (owner: O, ...args: unknown[]) => unknown;

// (No @packageDocumentation comment for this package)

```
